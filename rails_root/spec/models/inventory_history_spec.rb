require 'spec_helper'

describe InventoryHistory do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    InventoryHistory.create!(@valid_attributes)
  end
end
